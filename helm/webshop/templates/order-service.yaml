apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-order
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.order.port }}
      targetPort: {{ .Values.order.port }}
  selector:
    app: {{ .Release.Name }}-order
